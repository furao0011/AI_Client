# DONE_REQUEST

## v0.1.0

- 当前项目只是一个用来简单测试水平的玩具，之后的app开发完全移除当前的界面和逻辑重新开始。

- 我的目标是开发一个多界面、高可用的ai问答app，最初的版本需要包含登录界面、AI会话列表、AI对话板以及用户设置这几个最重要的部分。

## v0.1.1

- 美观地完成以下界面并整合起来：

  - **用户登录**: 安全的用户认证界面。
  
  - **AI会话列表**: 管理历史对话和新建会话。

## v0.1.2

- 美观地完成以下界面并整合起来：

    - **AI对话板**: 实时与AI进行交互。

    - **用户设置**: 个性化应用配置，自己通过整合市面上所有ai问答c端的优点进行实现。

## v0.1.3

- 完成以下开发任务：

    - 状态管理: 完善 ViewModel，处理真实的数据流。

    - 对于需要实现的具体业务，我们采用mock一些模拟数据和模拟交互流程的方法来实现。比如将一个用户信息写死在app中。

## v0.1.4

- 完成以下开发任务：

    - 一是修复一个界面bug，在app的问答界面中键盘弹起时，包含顶部栏在内整个界面都被顶上去了，我需要顶部消息和顶边栏不被弹上去

        - 逻辑是整个聊天板的长度随着键盘弹起时也发生变化，这样可以保证顶部消息不被遮挡，持续输入时又是不断从底部更新消息。

    - 二是对个人设置里的各个功能都进行VM逻辑上的实现

        - 账号管理
        - 通知
        - 明暗模式切换
        - 清除历史记录
        - 对整个app进行中文适配，设置中能够切换语言为en/zh

## v0.1.4.1

- 完成以下开发任务：

  - 一是语言切换

    - 应当是列出可选语言的列表窗口（zh/en）（中文/英文），然后持久化为固定的语言。
    - 应当整个app所有部分都做中文的兼容，包括登录界面（可以在登录界面增加一个语言切换的小入口，尽量做的协调美观）和其他的界面，

  - 二是明暗模式切换的逻辑没有实现

  - 三是账号管理的逻辑没有实现

## v0.1.4.2

- 完成以下开发任务：

  - 优化整体的界面布置，当前界面过于简单，需要更美观的界面UI设计。
  - 思考对话app需要的一些基本逻辑是否全部实现了，比如历史会话的删除方式，是否可以修改用户的历史消息让AI重新回答，一些异常处理是否有做相应的适配。

- 开发前先阅读DEV_LOG文件了解当前开发进度，同时阅读 Rule.md 的开发规则，严谨遵照开发规则，规范开发app。有重大更新时记得更新完善README文件。

## v0.1.5

- 完成以下任务：

    - 将Mock的逻辑逐步移植持久化到具体的客户端数据库上（google推荐room，基于SQLite原生的封装）。

## v0.1.5.1

- 完成以下任务：

  - 继续将Mock的逻辑逐步移植持久化到具体的客户端数据库上（google推荐room，基于SQLite原生的封装）。
  - 还有很多MockRepository的问题没有处理干净
  - 整体UI的很多设计仍要修改，比如编辑消息重新生成回答，弹出弹窗明显与主流的AI对话应用的编辑方式不同，尝试阅读整个架构找到类似这样设计不够fancy的部分，使之契合主流AI对话的设计且美观。

## v0.1.6

- 完成以下任务：

    - 添加网络层（Retrofit）连接真实 AI API
    - 使用OpenAI格式的API
    - API连接方式为：用户在app的个人设置中配置URL与sk，模型名称，选择固定的配置来调用API

## v0.1.6.1

- 完成以下任务：

    - 我希望消息气泡中能够解析markdown表达式并展示
    - 我希望加入一个逻辑：若是某个会话第一条发送的消息，该次请求的返回的文本将是一个格式化的数据，一个是用于回复的消息，一个是对该次会话总结的标题，作为该次会话的标题。
    - 我希望加入多模态输入，允许用户上传图像，并将图像作为消息的输入，这或许需要将图像转化成外链，这该如何实现？

## v0.1.6.2

- 完成以下任务：

    - 网络请求重试机制
    - 流式响应 (SSE Streaming) - AI 回复逐字显示
    - 消息复制功能
    - 多 API 配置管理（保存多个 API 配置切换使用）

## v0.1.6.3

- 完成以下任务：

    - 顶部栏改造：显示会话标题，支持下拉选择其他会话
    - 消息加载状态动画：等待响应时显示三点跳动加载动画

# TO_DO_REQUEST

## v0.1.6.4

- 完成以下任务：

    - 离线消息队列
    - 会话导出功能（导出为 Markdown/JSON）
    - 消息搜索功能